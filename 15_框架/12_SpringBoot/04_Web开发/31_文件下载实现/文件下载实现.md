# 文件下载实现

## 先说结论

Spring MVC 提供多种方式实现文件下载：ResponseEntity、StreamingResponseBody、InputStreamResource。

## 深度解析

### 方式1：ResponseEntity

```java
@GetMapping("/download/{filename}")
public ResponseEntity<Resource> download(@PathVariable String filename) {
    File file = new File("/uploads/" + filename);
    if (!file.exists()) {
        return ResponseEntity.notFound().build();
    }
    
    Resource resource = new FileSystemResource(file);
    
    HttpHeaders headers = new HttpHeaders();
    headers.add(HttpHeaders.CONTENT_DISPOSITION, 
            "attachment; filename=" + filename);
    
    return ResponseEntity.ok()
            .headers(headers)
            .contentType(MediaType.APPLICATION_OCTET_STREAM)
            .body(resource);
}
```

### 方式2：StreamingResponseBody

```java
@GetMapping("/download/{filename}")
public void download(@PathVariable String filename, 
                    OutputStream outputStream) {
    File file = new File("/uploads/" + filename);
    try (InputStream inputStream = new FileInputStream(file)) {
        IOUtils.copy(inputStream, outputStream);
        outputStream.flush();
    }
}
```

### 方式3：大文件下载

```java
@GetMapping("/download/{filename}")
public ResponseEntity<Resource> downloadLargeFile(@PathVariable String filename) {
    File file = new File("/uploads/" + filename);
    Resource resource = new FileSystemResource(file);
    
    return ResponseEntity.ok()
            .contentType(MediaType.APPLICATION_OCTET_STREAM)
            .header(HttpHeaders.CONTENT_DISPOSITION, 
                    "attachment; filename=\"" + filename + "\"")
            .contentLength(file.length())
            .body(resource);
}
```

## 关联知识点

- `30_MultipartFile文件上传`：文件上传
- `12_@ResponseBody响应体`：响应处理
