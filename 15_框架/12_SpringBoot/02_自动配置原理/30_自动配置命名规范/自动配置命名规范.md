# 自动配置命名规范

## 先说结论

Spring Boot 自动配置的命名遵循统一规范，包括配置类名、包结构、配置属性前缀等。遵循规范可以提高可读性、一致性，也是 Spring Boot 官方检查的要求。

## 深度解析

### 配置类命名

| 类型 | 命名规范 | 示例 |
|------|----------|------|
| 自动配置类 | `XxxAutoConfiguration` | `DataSourceAutoConfiguration` |
| 配置属性类 | `XxxProperties` | `DataSourceProperties` |
| 自动配置条件类 | `OnXxxCondition` | `OnClassCondition` |

### 包结构规范

```
org.springframework.boot.autoconfigure.
├── aop/                    → AopAutoConfiguration
├── data/                   → 数据访问相关
│   ├── jdbc/              → JdbcAutoConfiguration
│   ├── jpa/               → JpaAutoConfiguration
│   └── redis/             → RedisAutoConfiguration
├── transaction/            → TransactionAutoConfiguration
├── web/                   → Web 相关
│   └── servlet/          → Servlet Web
│   └── reactive/          → Reactive Web
```

### 属性配置命名

```java
@ConfigurationProperties(prefix = "spring.datasource")
public class DataSourceProperties {
    private String url;
    private String username;
    private String password;
    private int port;
}
```

对应配置：
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: 123456
    port: 3306
```

### Starter 命名

```
# 官方
spring-boot-starter-xxx

# 第三方（推荐）
xxx-spring-boot-starter

# 自动配置模块
xxx-spring-boot-autoconfigure
```

## 关联知识点

- [[自定义Starter自动配置]] — Starter 开发
- [[ConfigurationProperties配置绑定]] — 配置属性绑定
