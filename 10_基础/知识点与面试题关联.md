# 📎 Java 基础知识点与面试题关联文档

> **说明**：本文档将 `10_基础` 知识库中的 128 个知识点与面试题库（`E:\obsidian\Question\`）进行完整映射。
> - ✅ 已有对应题目（含题号与题目名）
> - ⚠️ 部分覆盖（有相关题目，但覆盖不完整）
> - ❌ 暂无对应题目（建议补充）
>
> 题目类型说明：`Q` = 问答题（01_Question）｜ `S` = 场景题（02_Scene）｜ `C` = 挖坑题（03_InQuestion）

---

## 一、Java 基础语法

### 1. Java 程序执行流程（.java → .class → JVM）
- ✅ 对应题目：
  - `Q-213` Java 运行时数据区（Runtime Data Area）

---

### 2. main 方法详解
- ❌ 暂无直接对应题目
- 💡 建议补充：「main 方法为什么必须是 public static void？能否重载 main 方法？」

---

### 3. 基本数据类型（8种）与默认值/占用字节数
- ✅ 对应题目：
  - `Q-212` Java 自动装箱与拆箱（含 Integer 缓存 -128~127）

---

### 4. 类型转换（自动 & 强制）
- ❌ 暂无直接对应题目
- 💡 建议补充：「自动类型转换和强制类型转换分别在什么场景下触发？精度丢失如何避免？」

---

### 5. 装箱与拆箱（Auto Boxing/Unboxing）
- ✅ 对应题目：
  - `Q-212` Java 自动装箱与拆箱（Integer 缓存池、拆箱导致 NPE）
- ✅ 挖坑题：
  - `C-41` 你用 `BigDecimal` 进行金额计算（涉及装箱精度问题）

---

### 6. 运算符（算术/关系/逻辑/位运算/三元运算符）
- ❌ 暂无直接对应题目
- 💡 建议补充：「位运算在实际开发中有哪些应用场景？（如权限掩码、HashMap 取模优化）」

---

### 7. 分支语句（if/else、switch-case、switch 新特性）
- ⚠️ 部分覆盖：
  - `Q-182` Java Sealed Class（涉及 switch 模式匹配）
- 💡 建议补充：「switch 表达式 vs switch 语句的区别？JDK 14+ switch 新特性如何使用？」

---

### 8. 循环语句（for/while/do-while/foreach）及 break/continue/return
- ❌ 暂无直接对应题目
- 💡 建议补充：「foreach 的底层实现是什么？在 foreach 中能否修改集合元素？为什么？」

---

### 9. 方法定义与调用
- ✅ 对应题目：
  - `Q-124` Java 方法调用底层机制（静态绑定 vs 动态绑定、虚方法表 vtable）

---

### 10. 方法参数传递（值传递 vs 引用传递）
- ✅ 对应题目（隐含）：
  - `Q-4` 深拷贝和浅拷贝的区别（涉及引用传递的理解）

---

### 11. 可变参数（Varargs）
- ❌ 暂无直接对应题目
- 💡 建议补充：「可变参数的底层实现是什么？在方法重载中可变参数的优先级如何？」

---

### 12. 静态导入（static import）
- ❌ 暂无直接对应题目
- 💡 建议补充：「静态导入有哪些注意事项？滥用静态导入会带来什么问题？」

---

## 二、面向对象（OOP）

### 1. 类与对象的关系
- ✅ 对应题目：
  - `Q-12` 对象在堆内存中的布局是怎样的？对象头里存储了什么信息？
  - `Q-213` Java 运行时数据区

---

### 2. 封装（Encapsulation）— 访问修饰符
- ❌ 暂无直接对应题目
- 💡 建议补充：「Java 四种访问修饰符的作用范围？在继承体系中如何影响子类访问？」

---

### 3. 继承（Inheritance）— extends
- ✅ 对应题目：
  - `Q-124` 方法调用底层机制（动态绑定/虚方法表）
  - `Q-125` Java 协变返回类型与桥接方法（泛型擦除后的继承问题）

---

### 4. 多态（Polymorphism）— 重写/重载
- ✅ 对应题目：
  - `Q-124` 静态绑定（早绑定）和动态绑定（晚绑定）
  - `Q-155` Java 中的方法重载
- ✅ 挖坑题（隐含）：
  - `C-9` 你写了一个工具方法，频繁拼接字符串（方法重载选择）

---

### 5. 抽象类（abstract class）vs 接口（interface）
- ⚠️ 部分覆盖：
  - `Q-177` Spring Boot 3 新特性（涉及接口默认方法改进）
  - `Q-35` 什么是注解（Annotation）？（注解本质是接口）
- 💡 建议补充：「抽象类和接口各自的适用场景？JDK 8+ 接口默认方法如何影响两者差异？」

---

### 6. 内部类（成员/静态/局部/匿名）
- ✅ 对应题目：
  - `Q-121` Java 的内部类（各种类型及应用场景）

---

### 7. 构造方法（构造函数）
- ❌ 暂无直接对应题目
- 💡 建议补充：「构造方法可以被继承吗？this() 和 super() 在构造方法中有什么约束？」

---

### 8. 代码块（构造代码块 vs 静态代码块 vs 普通代码块）
- ✅ 对应题目（隐含）：
  - `Q-42` 类的加载过程（静态代码块的执行时机）

---

### 9. this 关键字
- ❌ 暂无直接对应题目
- 💡 建议补充：「this 关键字的几种用法？匿名内部类中 this 和外部类 this 如何区分？」

---

### 10. super 关键字
- ❌ 暂无直接对应题目
- 💡 建议补充：「super 和 this 的使用限制有哪些？super() 调用有什么规定？」

---

### 11. static 关键字（静态属性/静态方法/静态代码块/静态内部类）
- ✅ 对应题目：
  - `Q-92` Java 的 `final` 关键字有哪些用法（涵盖 static final 组合）
  - `Q-42` 类的加载过程（静态初始化顺序）
  - `Q-124` 静态绑定（早绑定）
- ✅ 挖坑题：
  - `C-31` 你用 `synchronized` 实现了一个单例模式（静态方法 + 同步）
  - `C-37` 你用 `ThreadLocal` 存储用户信息（静态字段）

---

### 12. final 关键字（final 变量/方法/类）
- ✅ 对应题目：
  - `Q-92` Java 的 `final` 关键字有哪些用法（使用场景、不可变性）
  - `Q-66` 不可变对象一定是线程安全的吗？
  - `Q-215` volatile 关键字的底层实现（final 的内存语义）

---

## 三、异常处理

### 1. 异常体系（Throwable / Error / Exception）
- ✅ 对应题目：
  - `Q-33` Java 的异常体系（Checked vs Unchecked，项目中如何处理异常）
  - `Q-184` Java 异常性能开销

---

### 2. Checked Exception vs Unchecked Exception
- ✅ 对应题目：
  - `Q-33` Java 的异常体系（详细区分 Checked/Unchecked）

---

### 3. try-catch-finally 执行顺序（含 return 场景）
- ✅ 对应题目：
  - `Q-94` Java 中的 `try-catch-finally` 中，如果 `catch` 中有 return...
- ✅ 挖坑题：
  - `C-44` 你用 `try-catch` 捕获所有异常（finally 执行顺序陷阱）
  - `C-89` 你的异常处理

---

### 4. try-with-resources（自动资源关闭）
- ⚠️ 部分覆盖：
  - `C-44` 你用 `try-catch` 捕获所有异常（涉及资源关闭）
- 💡 建议补充：「try-with-resources 的底层原理？AutoCloseable 接口的 close() 调用顺序？多资源时异常如何处理（Suppressed Exception）？」

---

### 5. throw vs throws
- ⚠️ 部分覆盖（`Q-33` 有涉及，未深入）
- 💡 建议补充：「throw 和 throws 的区别？在重写方法时，子类方法的 throws 声明有什么限制？」

---

### 6. 自定义异常
- ⚠️ 部分覆盖（`Q-33` 有提及项目中异常处理，未独立出题）
- 💡 建议补充：「如何设计业务异常体系？自定义异常应该继承 RuntimeException 还是 Exception？为什么？」

---

### 7. 常见运行时异常（NPE/ClassCastException/ArrayIndexOutOfBoundsException 等）
- ✅ 对应题目：
  - `Q-212` 自动拆箱导致的 NPE（`Integer x = null; int y = x`）
- ✅ 挖坑题：
  - `C-37` ThreadLocal 使用不当导致的异常
  - `C-42` `Arrays.asList()` 导致 UnsupportedOperationException
- 💡 建议补充：「StackOverflowError 和 OutOfMemoryError 分别是什么？如何排查 NPE？」

---

### 8. 异常链（Exception Chaining）
- ❌ 暂无直接对应题目
- 💡 建议补充：「什么是异常链？Throwable 的 cause 如何传递？在微服务中异常链如何跨服务保留上下文？」

---

## 四、泛型

### 1. 泛型的本质（类型参数化）
- ✅ 对应题目：
  - `Q-34` Java 的泛型擦除（泛型本质分析）
  - `Q-152` Java 的泛型擦除发生在编译期还是运行时

---

### 2. 泛型类（Generic Class）
- ✅ 对应题目（隐含）：
  - `Q-34` 泛型擦除（涵盖泛型类的定义）

---

### 3. 泛型接口（Generic Interface）
- ⚠️ 部分覆盖：
  - `Q-34`（隐含）
- 💡 建议补充：「泛型接口在实现时有哪些方式？（指定具体类型/传递泛型参数）」

---

### 4. 泛型方法（Generic Method）
- ⚠️ 部分覆盖：
  - `Q-34`（隐含）
- 💡 建议补充：「泛型方法和泛型类的区别？泛型方法中的类型参数独立于类的类型参数？」

---

### 5. 泛型通配符（? extends T / ? super T / ?）
- ✅ 对应题目：
  - `Q-34` 泛型擦除（涉及通配符）
- 💡 建议补充：「无界通配符 `?` 和 `Object` 有什么区别？`List<?>` 和 `List<Object>` 的区别？」

---

### 6. 泛型擦除机制（Type Erasure）
- ✅ 对应题目：
  - `Q-34` 什么是 Java 的泛型擦除？`List<String>` 和 `List<Integer>` 运行时区别？
  - `Q-152` Java 的泛型擦除发生在编译期还是运行时

---

### 7. 泛型擦除后的类型桥接
- ✅ 对应题目：
  - `Q-125` 什么是桥接方法（Bridge Method）？泛型擦除后为什么需要生成桥接方法？

---

### 8. 泛型约束（不能实例化/不能创建泛型数组/静态方法不能使用类泛型）
- ⚠️ 部分覆盖（`Q-34` 有涉及，未深入）
- 💡 建议补充：「为什么不能创建泛型数组 `new T[]`？为什么静态方法不能使用类的类型参数？」

---

### 9. 泛型与反射结合
- ✅ 对应题目（隐含）：
  - `Q-61` Java 反射机制（反射获取泛型实际类型）

---

### 10. PESC 原则（Producer Extends, Consumer Super）
- ✅ 对应题目（隐含）：
  - `Q-34` 泛型通配符（上界/下界通配符）
- 💡 建议补充：「PECS 原则的记忆方法？在 JDK 标准库中（如 `Collections.copy`）PECS 是如何体现的？」

---

## 五、集合框架

### 1. Collection vs Collections 区别
- ❌ 暂无直接对应题目
- 💡 建议补充：「Collection 和 Collections 的区别？Collections 工具类常用方法有哪些？」

---

### 2. Iterable vs Iterator 区别
- ❌ 暂无直接对应题目
- 💡 建议补充：「Iterable 和 Iterator 的关系？Iterator 的 `remove()` 和集合的 `remove()` 有什么区别？」

---

### 3. List 接口（ArrayList/LinkedList/Vector）
- ✅ 对应题目：
  - `Q-2` ArrayList 和 LinkedList 的区别（核心 List 对比题）

---

### 4. ArrayList 底层实现（扩容机制）
- ✅ 对应题目：
  - `Q-2` ArrayList 和 LinkedList 的区别（含扩容机制分析）
- ✅ 挖坑题：
  - `C-42` 你用 `Arrays.asList()` 转换数组（ArrayList 陷阱）

---

### 5. LinkedList 底层实现（双向链表）
- ✅ 对应题目：
  - `Q-2` ArrayList 和 LinkedList 的区别（含 LinkedList 性能反例）

---

### 6. ArrayList vs LinkedList
- ✅ 对应题目：
  - `Q-2` ArrayList 和 LinkedList 的区别？什么场景下 LinkedList 反而比 ArrayList 慢？

---

### 7. Set 接口（HashSet/LinkedHashSet/TreeSet）
- ⚠️ 部分覆盖（知识点有，但面试题主要在 HashSet/LinkedHashSet/TreeSet 的底层原理）
- 💡 建议补充：「HashSet、LinkedHashSet、TreeSet 各自的使用场景？如何自定义 TreeSet 的排序规则？」

---

### 8. HashSet 底层实现（基于 HashMap）
- ✅ 对应题目（隐含）：
  - `Q-1` HashMap 的 put 方法流程（HashSet 底层即 HashMap）
  - `Q-93` Java 中的 equals 和 hashCode（HashSet 去重原理）

---

### 9. LinkedHashSet 实现原理（维护插入顺序）
- ✅ 对应题目：
  - `Q-214` LinkedHashMap 和 HashMap 的核心区别（LinkedHashSet 基于 LinkedHashMap）

---

### 10. TreeSet 底层实现（红黑树）
- ✅ 对应题目（隐含）：
  - `Q-95` Java 中的 `Comparable` 和 `Comparator` 有什么区别？（TreeSet 排序依赖）

---

### 11. Map 接口（HashMap/LinkedHashMap/TreeMap/Hashtable/ConcurrentHashMap）
- ✅ 对应题目：
  - `Q-1` HashMap put 方法流程
  - `Q-31` HashMap 扩容机制
  - `Q-32` ConcurrentHashMap JDK7 vs JDK8
  - `Q-214` LinkedHashMap 和 HashMap 的核心区别
  - `Q-211` HashMap 链表转红黑树阈值为什么是 8

---

### 12. HashMap 底层实现（JDK 1.7 数组+链表 / JDK 1.8+ 红黑树）
- ✅ 对应题目：
  - `Q-1` HashMap 的 put 方法流程（JDK8 vs JDK7 优化）
  - `Q-31` HashMap 的扩容机制（rehash 优化）
  - `Q-211` HashMap 在 JDK 8 中链表转红黑树的阈值为什么是 8？
- ✅ 挖坑题：
  - `C-10` 你的系统有一个热数据缓存，用 `HashMap` 存储（线程安全陷阱）

---

### 13. HashMap 与 Hashtable 的区别（线程安全/同步机制）
- ✅ 对应题目：
  - `Q-32` ConcurrentHashMap 在 JDK 7 和 JDK 8 中的实现区别（涵盖与 Hashtable 的对比）

---

### 14. Queue 接口（offer/poll/peek）
- ⚠️ 部分覆盖：
  - `Q-100` 什么是 `TransferQueue`？和 `BlockingQueue` 有什么区别？
- 💡 建议补充：「offer/add、poll/remove、peek/element 三组方法的区别？Queue 的 offer 和 add 在队满时行为不同？」

---

### 15. Deque 接口（双端队列）
- ⚠️ 部分覆盖（`Q-100` 涉及 BlockingQueue，Deque 独立无题）
- 💡 建议补充：「Deque 接口的常见实现有哪些？ArrayDeque 和 LinkedList 作为 Deque 哪个性能更好？」

---

### 16. fail-fast 与 fail-safe 机制
- ❌ 暂无直接对应题目
- 💡 建议补充：「什么是 fail-fast 机制？modCount 是如何工作的？fail-safe 的集合有哪些？CopyOnWriteArrayList 和普通 ArrayList 的迭代器有什么区别？」
- ✅ 挖坑题（隐含）：
  - `C-67` 你用 `CopyOnWriteArrayList` 实现读多写少的列表（fail-safe 的实现）

---

## 六、IO 与 NIO

> ⚠️ **注意**：此模块在知识地图中有独立章节，但知识点卡片尚未生成（01_基础语法目录下无 IO 文件夹）。以下为题目覆盖情况。

### 1. IO 分类（字节流/字符流、输入流/输出流）
- ❌ 暂无直接对应题目
- 💡 建议补充：「字节流和字符流的本质区别？InputStream 和 Reader 体系各有哪些核心类？」

### 2. 节点流 vs 处理流（装饰器模式）
- ❌ 暂无直接对应题目

### 3-4. InputStream/OutputStream 体系、Reader/Writer 体系
- ❌ 暂无直接对应题目
- 💡 建议补充：「IO 流的装饰器模式是如何实现的？FilterInputStream 的作用？」

### 5-6. 字节流转字符流、缓冲流
- ❌ 暂无直接对应题目

### 8. 对象序列化（Serializable/Externalizable）
- ✅ 对应题目：
  - `Q-64` 什么是 Java 的序列化？如何实现？serialVersionUID 的作用？

### 9. transient 关键字
- ✅ 对应题目（隐含）：
  - `Q-64` 序列化（transient 修饰的字段不参与序列化）

### 10. serialVersionUID 的作用
- ✅ 对应题目：
  - `Q-64` serialVersionUID 的作用（版本兼容控制）

### 11-16. NIO 三大核心（Channel/Buffer/Selector）、BIO/NIO/AIO 区别
- ❌ 暂无直接对应题目
- 💡 建议补充：「NIO 的三大核心组件如何配合工作？Selector 多路复用的原理是什么？BIO/NIO/AIO 在 Netty 中如何体现？」

---

## 七、注解（Annotation）

### 1. 注解的本质（元数据）
- ✅ 对应题目：
  - `Q-35` 什么是注解（Annotation）？Spring 中常用的注解有哪些？自定义注解如何实现？

### 2. JDK 内置注解（@Override/@Deprecated/@SuppressWarnings）
- ✅ 对应题目（隐含）：
  - `Q-35` 注解体系介绍

### 3. 元注解（@Target/@Retention/@Documented/@Inherited）
- ✅ 对应题目（隐含）：
  - `Q-35` 自定义注解如何实现（元注解的使用）

### 4. 自定义注解
- ✅ 对应题目：
  - `Q-35` 自定义注解如何实现
- ✅ 挖坑题（隐含）：
  - `C-86` 你的 Bean 注入（Spring 注解相关）

### 5. 注解的提取（反射 + Annotation 接口）
- ✅ 对应题目：
  - `Q-61` Java 反射机制（反射读取注解信息）

### 6. 注解的应用场景（Spring/@Component/@Autowired/JUnit）
- ✅ 对应题目：
  - `Q-35` Spring 中常用注解
  - `Q-86` @Transactional 事务失效场景

### 7. @FunctionalInterface 注解
- ✅ 对应题目：
  - `Q-62` Java 8 的 Lambda 表达式（函数式接口 @FunctionalInterface）

---

## 八、反射（Reflection）

### 1. 反射的原理与应用场景
- ✅ 对应题目：
  - `Q-61` 什么是 Java 的反射机制？反射的优缺点？在什么场景下使用？

### 2. Class 对象的获取方式（.class/forName/getClass）
- ✅ 对应题目（隐含）：
  - `Q-61` 反射机制（获取 Class 对象的方式）
  - `Q-102` 什么是 Java 的类加载过程（Class 对象与类加载器）

### 3-6. Class 常用方法、反射创建对象、调用方法、访问字段
- ✅ 对应题目：
  - `Q-61` Java 反射机制（综合覆盖）

### 7. 反射绕过泛型检查
- ✅ 对应题目（隐含）：
  - `Q-34` 泛型擦除（反射可以绕过泛型限制）

### 8. 反射与工厂模式/代理模式
- ✅ 对应题目：
  - `Q-91` Java 的动态代理（JDK 动态代理基于反射）
  - `Q-57` Spring AOP 原理（JDK 代理 vs CGLIB）

### 9. 反射的性能开销
- ✅ 对应题目：
  - `Q-184` Java 异常性能开销（与反射性能类比）
  - `Q-185` Java MethodHandle（相比反射的性能优势）

---

## 九、字符串（String）

### 1. String 不可变性（为什么设计为不可变）
- ✅ 对应题目：
  - `Q-3` String、StringBuilder、StringBuffer 的区别（String 不可变性分析）
  - `Q-66` 不可变对象一定是线程安全的吗？（String 是典型案例）

### 2. String、StringBuilder、StringBuffer 区别
- ✅ 对应题目：
  - `Q-3` String、StringBuilder、StringBuffer 的区别

### 3. String.intern() 方法
- ✅ 对应题目：
  - `Q-3` String 的 intern() 方法有什么作用？
  - `Q-154` Java 中 `String.intern()` 深入分析
- ✅ 挖坑题：
  - `C-69` 你用 `String.intern()` 节省内存（使用陷阱）

### 4. String 常用方法
- ❌ 暂无独立对应题目（常见方法使用散见于各题中）

### 5. String 的 + 拼接原理（编译期优化）
- ✅ 对应题目（隐含）：
  - `Q-3` String 性能分析
- ✅ 挖坑题：
  - `C-9` 你写了一个工具方法，频繁拼接字符串（StringBuilder 优化）

### 6. String 的哈希计算
- ✅ 对应题目（隐含）：
  - `Q-93` equals 和 hashCode 关系（String 的 hashCode 实现）

### 7. 字符串常量池（String Pool）
- ✅ 对应题目：
  - `Q-154` Java 中 `String.intern()` 深入（含常量池位置变迁）
  - `Q-3` String.intern() 方法作用

### 8. new String("xxx") 创建几个对象
- ✅ 对应题目（隐含）：
  - `Q-154` String.intern() 深入分析（涉及 new String 的对象创建分析）

---

## 十、Lambda 与函数式编程

### 1. Lambda 表达式语法
- ✅ 对应题目：
  - `Q-62` Java 8 的 Lambda 表达式是如何实现的？函数式接口是什么？

### 2. 函数式接口（@FunctionalInterface）
- ✅ 对应题目：
  - `Q-62` 函数式接口
  - `Q-35` @FunctionalInterface 注解

### 3. 四大核心函数式接口（Consumer/Supplier/Predicate/Function）
- ✅ 对应题目（隐含）：
  - `Q-62` Lambda 表达式（四大函数式接口介绍）

### 4. 方法引用（Method Reference）
- ✅ 对应题目（隐含）：
  - `Q-62` Lambda 表达式（方法引用语法）

### 5. 构造器引用
- ⚠️ 部分覆盖（`Q-62` 有涉及，但未深入）

### 6. Lambda 表达式作用域（访问局部变量/final 限制）
- ✅ 对应题目（隐含）：
  - `Q-62` Lambda 实现原理（捕获外部变量必须 effectively final）

---

## 十一、Stream API

### 1. Stream 的创建（Collection.stream/Arrays.stream/Stream.of）
- ✅ 对应题目：
  - `Q-63` Java 8 的 Stream API 有哪些常用操作？

### 2. 中间操作（filter/map/flatMap/distinct/sorted/limit/skip）
- ✅ 对应题目：
  - `Q-63` Stream API 常用操作

### 3. 终端操作（collect/forEach/count/min/max/sum/reduce）
- ✅ 对应题目：
  - `Q-63` Stream API 常用操作

### 4. Optional 与 Stream 结合
- ✅ 对应题目（隐含）：
  - `Q-123` Optional 是什么（Optional 的 stream() 方法）

### 5. 并行流（parallelStream）原理
- ✅ 对应题目：
  - `Q-63` 并行流（parallelStream）的原理和注意事项

### 6. Stream 惰性求值
- ⚠️ 部分覆盖（`Q-63` 有提及，未深入）
- 💡 建议补充：「Stream 惰性求值的底层实现？中间操作为什么不立即执行？」

### 7. Stream 的性能考虑
- ✅ 对应题目（隐含）：
  - `Q-63` 并行流注意事项（小数据量反而更慢等问题）

---

## 十二、其他核心概念

### 1. == vs equals()
- ✅ 对应题目：
  - `Q-93` Java 中的 `equals` 和 `hashCode`

### 2. hashCode() 与 equals() 关系
- ✅ 对应题目：
  - `Q-93` equals 和 hashCode 关系（覆盖规范）

### 3. Object 类方法与 Objects 工具类
- ✅ 对应题目（隐含）：
  - `Q-93` hashCode/equals（Object 核心方法）

### 4. 深拷贝 vs 浅拷贝
- ✅ 对应题目：
  - `Q-4` 深拷贝和浅拷贝的区别？如何实现一个对象的深拷贝？

### 5. clone() 方法与 Cloneable 接口
- ✅ 对应题目（隐含）：
  - `Q-4` 深拷贝（clone 方法的浅拷贝问题）

### 6. Comparable vs Comparator
- ✅ 对应题目：
  - `Q-95` Java 中的 `Comparable` 和 `Comparator` 有什么区别？

### 7. 枚举类（enum）
- ✅ 对应题目：
  - `Q-122` Java 的 `Enum` 枚举本质是什么？枚举可以实现接口吗？枚举单例模式有什么优势？

### 8. 枚举实现接口
- ✅ 对应题目：
  - `Q-122` 枚举可以实现接口吗？

### 9. 枚举单例模式
- ✅ 对应题目：
  - `Q-122` 枚举的单例模式有什么优势？和双重检查锁单例相比有什么区别？

### 10. System 类常用方法（arraycopy/exit/gc/currentTimeMillis）
- ✅ 对应题目（隐含）：
  - `C-40` 你用 `System.currentTimeMillis()` 获取时间戳（性能陷阱）
  - `C-73` 你用 `System.gc()` 主动触发 GC（使用问题）

### 11. Math 类常用方法
- ❌ 暂无直接对应题目
- 💡 建议补充（可选）：「Math.round / Math.floor / Math.ceil 的区别？」

### 12. Random 类的线程安全问题
- ✅ 对应题目：
  - `Q-191` Java ThreadLocalRandom（高性能随机数）

### 13. ThreadLocalRandom（高性能随机数）
- ✅ 对应题目：
  - `Q-191` Java ThreadLocalRandom 的实现原理

### 14. BigDecimal 浮点数精度问题
- ✅ 对应题目（隐含）：
  - `C-41` 你用 `BigDecimal` 进行金额计算（精度问题踩坑）

### 15. Date/LocalDateTime/Instant 时间处理
- ❌ 暂无直接对应题目
- 💡 建议补充：「Java 8 之前的 Date 有什么问题？LocalDateTime 和 ZonedDateTime 的区别？」

### 16. Arrays 工具类常用方法
- ✅ 对应题目（隐含）：
  - `C-42` 你用 `Arrays.asList()` 转换数组（常见陷阱）

---

## 十三、Java 新特性（JDK 8~21）

### 1. 接口默认方法与静态方法（JDK 8）
- ✅ 对应题目（隐含）：
  - `Q-5` 抽象类 vs 接口（接口默认方法改变了两者差异）
  - `Q-177` Spring Boot 3 新特性（Java 新版本要求）

### 2. 接口私有方法（JDK 9）
- ❌ 暂无直接对应题目
- 💡 建议补充：「JDK 9 接口私有方法解决了什么问题？」

### 3. var 局部变量类型推断（JDK 10）
- ❌ 暂无直接对应题目
- 💡 建议补充：「var 关键字的限制是什么？和 JavaScript 的 var 有什么区别？」

### 4. 集合工厂方法（List.of/Set.of/Map.of）（JDK 9）
- ❌ 暂无直接对应题目
- 💡 建议补充：「List.of() 和 Collections.unmodifiableList() 的区别？List.of() 允许 null 吗？」

### 5. Optional 类（JDK 8）
- ✅ 对应题目：
  - `Q-123` Optional 是什么（使用场景、与 null 检查的对比）

### 6. String 改进（join/lines/strip/repeat/isBlank）（JDK 11）
- ✅ 对应题目（隐含）：
  - `Q-196` Java Compact Strings（String 底层优化）

### 7. Switch 表达式（JDK 12 预览/14 正式）
- ❌ 暂无直接对应题目
- 💡 建议补充：「switch 表达式和传统 switch 语句的区别？switch 表达式的返回值如何处理？」

### 8. Text Blocks 文本块（JDK 13 预览/15 正式）
- ❌ 暂无直接对应题目

### 9. Pattern Matching for instanceof（JDK 16 正式）
- ❌ 暂无直接对应题目
- 💡 建议补充：「instanceof 模式匹配的语法？和传统 instanceof + 强转相比有何优势？」

### 10. Records 记录类（JDK 16 正式）
- ✅ 对应题目：
  - `Q-181` Java Record 类（定义、与传统 POJO 对比、限制）

### 11. Sealed Classes 密封类（JDK 17 正式）
- ✅ 对应题目：
  - `Q-182` Java Sealed Class（密封类的设计目的、与 switch 配合使用）

### 12. 模式匹配 for switch（JDK 21 正式）
- ✅ 对应题目（隐含）：
  - `Q-182` Java Sealed Class（与 switch 模式匹配结合使用）

---

## 📋 建议新增题目清单

> 以下知识点在题库中**完全空白或严重不足**，建议优先补充面试题。

### 🔴 高优先级（高频考点，完全无题）

| # | 知识点 | 建议题目 |
|---|--------|---------|
| 1 | IO/NIO 核心体系 | NIO 三大核心组件（Channel/Buffer/Selector）如何配合工作？BIO/NIO/AIO 的区别及适用场景？ |
| 2 | fail-fast vs fail-safe | modCount 机制是什么？ArrayList 迭代时删除元素为什么会抛 ConcurrentModificationException？CopyOnWriteArrayList 是如何解决这个问题的？ |
| 3 | LinkedHashMap 底层实现 | LinkedHashMap 如何维护插入顺序和访问顺序？如何用 LinkedHashMap 实现 LRU 缓存？ |
| 4 | try-with-resources 深入 | AutoCloseable 的 close() 执行顺序？如果 close() 本身抛异常，和原始异常如何处理（Suppressed Exception）？ |
| 5 | 抽象类 vs 接口深入对比 | JDK 8+ 之后抽象类和接口还有什么本质区别？什么场景下必须用抽象类而不是接口？ |

### 🟡 中优先级（有一定考察频率）

| # | 知识点 | 建议题目 |
|---|--------|---------|
| 6 | Queue/Deque 接口体系 | ArrayDeque 和 LinkedList 作为 Deque 的性能对比？offer/add/put 方法的区别？ |
| 7 | 自定义异常设计 | 如何设计一套业务异常体系？异常码如何规划？全局异常处理如何实现？ |
| 8 | 异常链（Exception Chaining） | initCause() 和构造方法传入 cause 的区别？跨服务调用时异常信息如何保留上下文？ |
| 9 | 泛型通配符深入（? vs Object） | `List<?>` 和 `List<Object>` 有什么区别？无界通配符为什么不能 add？ |
| 10 | PECS 原则 | PECS 的完整理解？`Collections.copy(dest, src)` 的参数类型为什么是 `super T` 和 `extends T`？ |
| 11 | 类型转换（自动/强制）陷阱 | int 转 byte 的精度丢失是怎么发生的？隐式类型转换在哪些场景下会出问题？ |
| 12 | Stream 惰性求值原理 | Stream 的 pipeline 是如何构建和执行的？为什么中间操作不立即执行？ |

### 🟢 低优先级（补充完整性）

| # | 知识点 | 建议题目 |
|---|--------|---------|
| 13 | main 方法详解 | main 方法可以重载吗？JVM 如何找到并调用 main 方法？ |
| 14 | 可变参数（Varargs） | 可变参数的底层实现是数组吗？重载时可变参数方法的优先级？ |
| 15 | 封装与访问修饰符 | Java 四种访问修饰符的作用范围（类/包/子类/所有）？protected 在继承中的访问规则？ |
| 16 | 构造方法深入 | this() 和 super() 为什么必须在第一行？静态工厂方法相比构造方法有什么优势？ |
| 17 | LocalDateTime 时间处理 | LocalDateTime、ZonedDateTime、Instant 的区别？如何做时区转换？ |
| 18 | switch 表达式（JDK 14+） | switch 表达式和传统 switch 语句的区别？yield 关键字的作用？ |
| 19 | 集合工厂方法 List.of | List.of() 和 Arrays.asList() 的区别？不可修改的集合和不可变集合有区别吗？ |

---

## 📊 覆盖率统计

| 模块 | 知识点总数 | 已有题目 | 部分覆盖 | 无题目 | 覆盖率 |
|------|-----------|---------|---------|--------|--------|
| 一、基础语法 | 12 | 3 | 2 | 7 | 42% |
| 二、面向对象 | 12 | 5 | 3 | 4 | 67% |
| 三、异常处理 | 8 | 3 | 3 | 2 | 75% |
| 四、泛型 | 10 | 6 | 3 | 1 | 90% |
| 五、集合框架 | 16 | 9 | 4 | 3 | 81% |
| 六、IO 与 NIO | 16 | 3 | 0 | 13 | 19% |
| 七、注解 | 7 | 6 | 0 | 1 | 86% |
| 八、反射 | 9 | 8 | 0 | 1 | 89% |
| 九、字符串 | 8 | 6 | 0 | 2 | 75% |
| 十、Lambda | 6 | 4 | 1 | 1 | 83% |
| 十一、Stream | 7 | 5 | 1 | 1 | 86% |
| 十二、其他核心 | 16 | 10 | 1 | 5 | 69% |
| 十三、Java新特性 | 12 | 4 | 2 | 6 | 50% |
| **合计** | **139** | **72** | **20** | **47** | **66%** |

> **说明**：覆盖率 = (已有题目 + 部分覆盖×0.5) / 知识点总数

---

*文档生成时间：2026-04-22*
*关联题库版本：01_Question（Q1~Q224）、02_Scene（S1~S90）、03_InQuestion（C1~C90）*
